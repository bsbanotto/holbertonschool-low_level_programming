#include <stdio.h>

/**
 * main - contains if statements for required responses
 * a - alphabet character
 *
 * Return: 0 if it works
 */

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
		putchar (a);
	}
	putchar ('\n');

	return (0);
}
